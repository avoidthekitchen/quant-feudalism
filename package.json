{
  "name": "quant-feudalism",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "preview": "npx vite preview",
    "deploy": "npx vite build && npx wrangler deploy"
  },
  "dependencies": {
    "phaser": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "wrangler": "^4.86.0"
  }
}
